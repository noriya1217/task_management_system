<div class='admin_backcolor'>
  <div class='row field'>
    <div class='col-md-12'>
      <h2>管理画面【ユーザー詳細画面】</h2>
      <h3><%= @user.name %>のページ</h3>
      <p>メールアドレス: <%= @user.email %></p>
      <p>管理者権限：
        <% if @user.admin.user_id.nil? %>
          無
        <% else %>
          有
        <% end %>
      </p>
      <%= link_to 'ユーザー一覧', admin_users_path, class: 'btn btn-success btn-sm' %>
      <%= link_to 'ユーザー編集', edit_admin_user_path(@user.id), class: 'btn btn-warning btn-sm' %>
    </div>
  </div>
</div>